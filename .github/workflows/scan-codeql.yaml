name: '🔬 Run CodeQL'

on:
  push:
    branches: [ master ]
    paths-ignore:
      - '**/*.md'
  pull_request:
    branches: [ master ]
    paths-ignore:
      - '**/*.md'
  schedule:
    - cron: '24 2 * * 4'
  workflow_dispatch:

jobs:
  call:
    uses: JPKribs/jellyfin-plugin-episodepostergenerator/.github/workflows/scan-codeql.yaml@master
    with:
      repository-name: JPKribs/jellyfin-plugin-episodepostergenerator
